---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';
---

<header>
	<main>
	<nav>
		<div class="navlink">
			<!-- <img class="nav-logo" src="akhil-penguin-logo.png" alt=""> -->
			<a class="nav-logo-text text-4xl" href="/">{SITE_TITLE}</a>
		</div>
		<div class="navlink">
			<HeaderLink class="mx-4 sm:text-2xl text-base" href="/">Home</HeaderLink>
			<HeaderLink class="mx-4 sm:text-2xl text-base" href="/project">Projects</HeaderLink>
			<HeaderLink class="mx-4 sm:text-2xl text-base" href="/blog">Blogs</HeaderLink>
		</div>
	</nav>
	</main>
</header>
<style>
	header {
		margin: 0;
		background: var(--background);
		box-shadow: 0 2px 8px rgba(var(--black), 5%);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
	}
	nav {
		display: flex;
		margin-top: 4rem;
		align-items: center;
		flex-wrap: wrap;
		justify-content: space-between; 
	}
	nav a {
		color: var(--black);
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none; 
		color: var(--accent);
	}
	.social-links,
	.social-links a {
		display: flex;
	}

	.navlink{
		display:inline;;
		text-align: center;
	}

	.nav-logo{
		height: 2rem;
	}

	.nav-logo-text{
		font-family: 'FunnelDisplay', 'Times New Roman', Times, serif;
		font-weight: 700;
	}

	.color-primary{
		color: var(--accent);
	}

	@media (max-width: 720px) {
		.social-links {
			display: none;
		}

		.navlink{
			width: 100%;
		}

		nav {
			margin-top: 0rem;
			justify-content: space-between;
			border-bottom: 1px solid #00000031;
		}
	}
</style>
